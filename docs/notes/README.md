# Historical notes

Kept for archaeology, not maintained — they describe the repo at the moment they were written.

- `PLAN.md` — the deployment roadmap as of the split-mode work (server + agent over the link).
- `HANDOFF.md` — the step-by-step build log, written as each step landed.
- `understand.md`, `evaluate.md` — the idea-funnel passes that started the project, about the
  narrower predecessor idea (a read-mostly map of an existing codebase).

The live docs are [`README.md`](../../README.md), `CONTRACTS.md`, `docs/vision.md` and
`docs/onboarding.md`.
