---
title: Rename the onboarding skill from visual-harness to visualize
status: done
owner: builder4
---
## Goal
The repo-shipped onboarding skill (currently `skills/visual-harness/`, installed globally as a symlink at `~/.claude/skills/visual-harness`) should be named **visualize**. Same behavior, new name everywhere.

## Change
1. Rename the repo directory `skills/visual-harness/` → `skills/visualize/` (git mv).
2. Update `SKILL.md` frontmatter: `name: visualize`. Sweep the skill's own files (SKILL.md + helper scripts) and any repo references (HANDOFF.md, docs) for the old skill name / paths (`skills/visual-harness`, `skill://visual-harness`, `~/.claude/skills/visual-harness`) and update them. Do NOT rename the project/repo itself — only the skill.
3. Update the documented install one-liner in SKILL.md's `## Install` section to `ln -s "$PWD/skills/visualize" ~/.claude/skills/`.
4. Re-install on this machine: remove the old `~/.claude/skills/visual-harness` symlink, create `~/.claude/skills/visualize` → repo `skills/visualize`.
5. The skill's HARNESS resolution (git toplevel of the skill dir) is path-derived, so it must keep working through the new symlink — verify, don't assume.

## Acceptance
- `skills/visualize/SKILL.md` exists with `name: visualize`; `skills/visual-harness/` is gone.
- `grep -r "skills/visual-harness"` in the repo returns nothing (issue files in `.issues/` exempt — historical record, leave them).
- `~/.claude/skills/visualize` is a symlink into the repo and resolves; old `~/.claude/skills/visual-harness` symlink removed.
- Smoke: from a directory outside the repo, read SKILL.md through the new symlink and confirm HARNESS resolves to the checkout. Do NOT run project-wide test suites/formatters.

## Result
Merged to main at 8cca6e0 (`git mv skills/visual-harness skills/visualize`; SKILL.md `name: visualize`, comment + install one-liner updated). Repo-wide `skills/visual-harness` grep clean outside `.issues/`. Symlink reinstalled: `~/.claude/skills/visualize` → repo `skills/visualize`, old link removed. Smoke from `/tmp`: SKILL.md readable through symlink, documented HARNESS resolution (`git -C "$(dirname …/SKILL.md)" rev-parse --show-toplevel`) returns `/Users/orgal/code/visual-harness`.
