---
title: Pin pnpm via packageManager field (corepack)
status: done
owner: builder2
---
Recommended by builder4 while working #10: repo behavior now depends on the pnpm major (pnpm 11's `allowBuilds` schema; `onlyBuiltDependencies` ignored). Without a pin, the next pnpm config schema change reproduces the exact fresh-worktree breakage #10 fixed.

## Change
Add a `packageManager` pin to the root `package.json` (`"packageManager": "pnpm@<exact version in use>"` — read the installed version, don't guess) so corepack-enabled environments resolve the same pnpm. Confirm the pinned version is the one the #10 `allowBuilds` config targets.

## Acceptance
- Root package.json carries an exact `packageManager: pnpm@x.y.z` matching the locally installed pnpm.
- `pnpm install` still completes clean in the repo (no schema warnings, no churn).
- No project-wide suites/formatters.

## Result
Pinned `"packageManager": "pnpm@11.6.0"` in root package.json (923f43f, merged to main). Matches installed pnpm and the pnpm-workspace.yaml `allowBuilds` schema from #10. `pnpm install` clean: no schema warnings, no lockfile churn.
