{
  "name": "@shape/link",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "The link: Shape's canvas tool as an MCP server, plus a harness hook that reports what the agent is doing",
  "scripts": {
    "mcp": "node src/mcp.ts",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/ws": "^8.18.0",
    "typescript": "^5.7.0"
  }
}
